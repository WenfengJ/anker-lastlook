# LastLook 工程验收测试清单

## 验收范围

本清单用于验证 LastLook Demo 是否可以稳定完成现场演示。当前版本使用本地后台和模拟设备，不依赖真实 eufy SDK。

## P0 测试项

| 状态 | 测试项 | 操作步骤 | 预期结果 |
| --- | --- | --- | --- |
| DONE | 服务启动 | 执行 `npm start` | 页面可通过 `http://127.0.0.1:4173` 访问 |
| DONE | 健康检查 | 请求 `GET /api/health` | 返回 `ok: true` |
| DONE | 设备状态 | 请求 `GET /api/devices` | 摄像头在线，隐私区已设置 |
| DONE | 已确认场景 | 选择“已确认”，点击“开始出门检查” | 结果显示“灶台已确认关闭” |
| DONE | 无法确认场景 | 选择“无法确认”，点击“开始出门检查” | 结果显示“暂时无法确认灶台状态” |
| DONE | 异常场景 | 选择“异常”，点击“开始出门检查” | 结果显示“疑似有旋钮未关闭” |
| DONE | 证据照片 | 查看任一结果页 | 展示当前照片和全关基准照片 |
| DONE | 检查步骤 | 点击“开始出门检查” | 展示离家状态、预设位、拍照、比对、生成结果流程 |
| DONE | 控制台错误 | 浏览器完成三种场景验证 | 无新增前端错误 |

## 自动验证

执行：

```bash
npm test
```

当前自动验证覆盖：

- `/api/health`
- `/api/devices`
- 三种 Demo 场景切换
- 检查启动
- 检查结果读取
- 结果状态、标题和证据字段

## 手动验证结果

已在浏览器中完成三种场景真实点击验证：

- 已确认：通过
- 无法确认：通过
- 异常：通过

## 当前结论

LastLook Demo 已满足本地演示条件。下一步可以进入参赛 PPT 和 90 秒讲稿整理。
